spring:
  config:
    import: optional:file:../../infra/.env[.properties]
  data:
    mongodb:
      uri: mongodb://${DB_MONGO_USER}:${DB_MONGO_PASSWORD}@${DB_MONGO_HOST:localhost}:${DB_MONGO_PORT:27017}/${DB_MONGO_SOCIAL_NAME}?authSource=admin

grpc:
  server:
    port: ${GRPC_SERVER_PORT:9091}